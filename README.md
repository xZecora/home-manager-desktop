# Vitrial's Home Manager Config

## Notes

In case you don't read the entire `home.nix` first, `modules/neovim` currently does nothing and I'm just keeping it around in case `nvf` doesn't meet my needs suddenly
